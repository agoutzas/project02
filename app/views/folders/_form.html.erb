<%= form_for @folder do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :resource %>
  <%= f.select :resource_ids, Resource.pluck(:title, :id), :include_blank => true %>



  <%= f.submit %>
<% end %>
